/**
 * Config Manager (Legacy)
 * This file now imports from the refactored module structure for backward compatibility
 * @deprecated Import from './config/' directly for new code
 */

// Re-export the new ConfigManager for backward compatibility
module.exports = require('./config/');
