/**
 * Wizard (Legacy)
 * This file now imports from the refactored module structure for backward compatibility
 * @deprecated Import from './wizard/' directly for new code
 */

// Re-export the new Wizard for backward compatibility
module.exports = require('./wizard/');
