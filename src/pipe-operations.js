/**
 * Pipe Operations (Legacy)
 * This file now imports from the refactored module structure for backward compatibility
 * @deprecated Import from './pipe-operations/' directly for new code
 */

// Re-export the new PipeRegistry as PipeOperations for backward compatibility
module.exports = require('./pipe-operations/');
